# Production Deployment - Expense Tracker

## ✅ Production Ready!

Your application is now configured for production deployment on Render.com.

---

## 📋 What's Been Configured

### ✅ Build Process
- Production build script: `pnpm build`
- Client build: Vite bundles React app to `dist/public/`
- Server build: esbuild bundles Node.js server to `dist/index.js`
- Start command: `pnpm start` (runs `node dist/index.js`)

### ✅ Render Configuration
- `render.yaml` configured with production settings
- Node.js version specified (`.nvmrc`)
- Health check path configured
- Environment variables template ready

### ✅ Production Optimizations
- Static file serving with caching headers
- Production error handling
- Proper path resolution for built files
- SPA routing support

---

## 🚀 Quick Deploy Steps

### 1. Push to GitHub
```bash
git add .
git commit -m "Production ready"
git push origin main
```

### 2. Deploy on Render
1. Go to https://dashboard.render.com/
2. Click **New +** → **Web Service**
3. Connect repository
4. Render auto-detects `render.yaml` ✅
5. Set environment variables (see below)
6. Click **Create Web Service**

### 3. Required Environment Variables

Set these in Render dashboard → **Environment** tab:

```
GOOGLE_SERVICE_ACCOUNT_JSON=<full-json-as-single-line>
OAUTH_SERVER_URL=https://your-oauth-server.com
VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com
VITE_APP_ID=your-app-id
```

**For testing (mock OAuth):**
```
MOCK_OAUTH=true
```

### 4. Share Google Sheet
- Share your Google Sheet with the service account email
- Grant **Editor** access

---

## 📚 Documentation

- **Quick Start**: See `RENDER_DEPLOYMENT.md`
- **Detailed Guide**: See `PRODUCTION_DEPLOYMENT.md`
- **General Info**: See `README.md`

---

## 🔍 Pre-Deployment Checklist

- [ ] Code committed and pushed to GitHub
- [ ] Build works locally (`pnpm build`)
- [ ] Production server starts (`pnpm start`)
- [ ] All environment variables ready
- [ ] Google Sheet shared with service account
- [ ] OAuth provider configured (if not using mock)

---

## 🎯 Build Output

After `pnpm build`:
- `dist/public/` - Frontend static files (HTML, CSS, JS)
- `dist/index.js` - Backend server bundle

The server serves `dist/public/` in production mode.

---

## ⚙️ Environment Variables Reference

| Variable | Required | Description |
|----------|----------|-------------|
| `NODE_ENV` | ✅ | Set to `production` |
| `PORT` | ✅ | Server port (default: 10000 on Render) |
| `JWT_SECRET` | ✅ | Auto-generated by Render |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | ✅ | Google Sheets credentials |
| `OAUTH_SERVER_URL` | ⚠️ | Required if `MOCK_OAUTH=false` |
| `VITE_OAUTH_PORTAL_URL` | ⚠️ | Required if `MOCK_OAUTH=false` |
| `VITE_APP_ID` | ⚠️ | Required if `MOCK_OAUTH=false` |
| `MOCK_OAUTH` | ❌ | Set to `true` for testing |

---

## 🐛 Troubleshooting

**Build fails?**
- Check `pnpm-lock.yaml` is committed
- Verify Node.js 18+ in Render settings

**App won't start?**
- Check logs in Render dashboard
- Verify `PORT` is set correctly
- Ensure `dist/` directory exists after build

**Static files not loading?**
- Verify `dist/public/` exists
- Check build completed successfully
- Review server logs for path errors

---

## 📞 Support

- Render Docs: https://render.com/docs
- Render Support: https://render.com/docs/support
- Project Issues: Check `PRODUCTION_DEPLOYMENT.md`

---

**Ready to deploy! 🚀**

