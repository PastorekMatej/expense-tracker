# Quick Start: Deploy to Render

## 🚀 5-Minute Deployment

### Step 1: Push to GitHub
```bash
git add .
git commit -m "Production ready for Render"
git push origin main
```

### Step 2: Create Render Service

1. Go to https://dashboard.render.com/
2. Click **New +** → **Web Service**
3. Connect GitHub (if needed)
4. Select repository: `expense-tracker`
5. Render auto-detects `render.yaml` ✅
6. Click **Create Web Service**

### Step 3: Set Environment Variables

In Render dashboard → **Environment** tab, add:

#### Required (for production):
```
GOOGLE_SERVICE_ACCOUNT_JSON=<paste-full-json-here>
OAUTH_SERVER_URL=https://your-oauth-server.com
VITE_OAUTH_PORTAL_URL=https://your-oauth-portal.com
VITE_APP_ID=your-app-id
```

#### For Testing (use mock OAuth):
```
MOCK_OAUTH=true
```

**Note:** `JWT_SECRET` is auto-generated by Render ✅

### Step 4: Share Google Sheet

1. Open your Google Sheet
2. Click **Share**
3. Add service account email (from JSON → `client_email`)
4. Give **Editor** access
5. Click **Send**

### Step 5: Deploy

Click **Manual Deploy** → **Deploy latest commit**

Wait 2-5 minutes, then visit: `https://expense-tracker-xxxx.onrender.com`

---

## ✅ Verification Checklist

- [ ] Build completes successfully
- [ ] App loads at Render URL
- [ ] Sign-in works (mock or real OAuth)
- [ ] Can add expenses
- [ ] Expenses appear in Google Sheet
- [ ] Summaries show correct data

---

## 🔧 Troubleshooting

**Build fails?**
- Check logs in Render dashboard
- Verify `pnpm-lock.yaml` is committed
- Ensure Node.js 18+ is selected

**App won't start?**
- Check `PORT` is set to `10000`
- Verify all required env vars are set
- Check logs for errors

**Google Sheets not working?**
- Verify sheet is shared with service account
- Check `GOOGLE_SERVICE_ACCOUNT_JSON` format (single line, valid JSON)
- Ensure service account has Sheets API enabled

---

See `PRODUCTION_DEPLOYMENT.md` for detailed guide.


